# Config

Requirements
```
git
stow
terminator
terminator-themes
fish
oh-my-posh
```

Found a problem where terminator doesn't detect the OS dark mode. Fixed with `gnome-tweaks` and `gnome-themes-extra` enabling that theme for Legacy apps in Tweaks.
